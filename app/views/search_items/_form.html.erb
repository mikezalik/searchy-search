<%= form_with(model: search_item, local: true) do |form| %>
  <% if search_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_item.errors.count, "error") %> prohibited this search_item from being saved:</h2>

      <ul>
        <% search_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_area :link %>
  </div>

  <div class="field">
    <%= form.label :keywords %>
    <%= form.text_area :keywords %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
